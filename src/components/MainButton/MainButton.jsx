import './button.css'

function MainButton({value}) {
    return (
        <div>
            <button className='btn' value={value}>ENTRAR</button>
        </div>
        
    )
}

export default MainButton